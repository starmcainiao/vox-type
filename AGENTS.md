# vox-type · 项目级 AGENTS.md

> 本文件是**项目纪律与验收总纲**，给 AI 执行者（OpenCode 等）与贡献者看。
> 每一层另有自己的 `AGENTS.md`（固定七项），层的文档是写码基底与验收依据；本文件只管跨层的规矩。

## 一、项目一句话

**预铸语音资产层 + 双工行为控制器**：让语音链路的固定部分零延迟、零成本、可审核；不确定的部分才动用模型。
定位：**可短路语音栈的覆盖层**（级联链路 = inline 过滤器；端到端模型 = 轮级旁路）。

## 二、不动摇的纪律

1. **先规格后代码**：任何实现前，对应层 `AGENTS.md` 与任务卡必须已存在且写明验收标准。
2. **每层只认上一层产物**（D2）：跨层偷看 = 违规。规则层不知道音频，执行层不关心话术是谁写的。
3. **内核冻结 / 扩展分区**（D9）：`core/ rules/ compiler/ assets/ runtime/ eval/` = 冻结区（改动须走大版本 + 迁移期）；`adapters/ packs/ trigger/` = 扩展区（随便加）。**业务侧只碰 `packs/`，永不改内核**。
4. **禁止静默降级**：任何"本该命中却换了路径"的情况必须报错或留痕计数（fail-closed）。语音链路的静默降级表现为"突然换了个声音"，事后无法定位。
5. **一切产物可质检**：每层产物带 schema + 自带 `checks/`；CLI 一条命令跑全部质检，输出机器可读 JSON。
6. **数字必须有出处**：任何对外引用（延迟、命中率、成本）必须来自 `eval/` 的**可复现**报告（固定语料 + 固定种子），并标注统计口径（P50/P99、样本量、置信区间）。
7. **不写他人的活**：执行器不做语义分析；适配器不含业务逻辑；评测不改产品代码。
8. **宿主无关**（用户拍板 2026-09-23）：本仓是**前置包**，**不依附任何框架**——
   接入外部框架永远是「我们写一个 `adapters/framework_*` 去适配它」，**不是改内核迁就它**；
   引入第三方必须走完「变成部件 + 说清引用 + 留口子」三步（细则见 `docs/12 §12.15`）。
   推论：换宿主 = 新写一个适配器，`compiler/ assets/ runtime/` 一行不改；
   **「任意 Agent 需要语音时都能从这里取」靠的是能力接口，不是框架绑定**。

## 三、目录与冻结状态

```
README.md        对外说明 + 索引
AGENTS.md        本文件（项目级）
docs/            设计基础（讨论稿公开版）+ docs/tasks/ 任务卡
core/    [冻]    协议定义、原语集合、执行语义、质检规则引擎、指标口径
rules/   [冻]    五类规则、提示词（skill）、示例剧本
compiler/[冻]    预铸流水线 + 四属性校验器 + 差量重铸
assets/  [冻]    资产包格式与索引（格式冻结，内容随业务）
runtime/ [冻]    执行器与旁路：短路判定、槽位拼接、播放、双工参数
adapters/[扩]    tts-* / asr-* / framework-*（薄适配器）
packs/   [扩]    业务包：一业务一目录（话术 + 剧本 + 参数）
trigger/ [扩]    前置包触发器（state→plan）与轮级留痕（2026-09-19 自 adapters/state_trigger/ 迁入，T28）
eval/    [冻]    离线对拍与盲测、golden set、拼接自然度基准
cli/     [冻]    薄壳：解析参数 → 调各层**公开** API → 打印 → 定退出码；**不含任何判定逻辑**
                （命中判定/四属性/质检阈值/统计口径一律调对应层）；口径见 `docs/08-CLI口径.md`
bin/vox          CLI 入口（POSIX sh 薄壳，仓库内可直接跑，不依赖安装）
labs/    [实验]  一次性实验与外部对拍（**不进任何层的契约**）；产物自带口径与原始数据，
                例如 labs/chain-bench/（链路级语音对拍，见 docs/09）
tools/   [扩]    仓外数据获取与派生（只写仓外，不进任何层契约）
examples/[扩]    新人示例：plan 与语料真文件（`plan.json` / `bench-corpus.json`），
                `README.md` 与 `docs/22` 指向这里（T42，2026-09-23）
```

## 四、CLI 约定（命令名冻结；`docs/08-CLI口径.md` 定行为、退出码与错误分类）

```
vox pack build   <pack>     预铸：话术 → 资产包（含质检报告）
vox pack check   <pack>     只校验不预铸（源格式 + 五类规则 + 四属性）
vox run          <plan>     执行一条播报计划（快路；未命中默认 fail-closed）
vox bench        <pack>     离线对拍：快路 vs 慢路，出报告
vox verify       <artifact> 对任意产物跑质检（资产包/报告/剧本）
```

退出码（冻结）：`0` 成功 / `2` 用法或参数错误 / `3` 运行期失败（stderr 必带异常类型名）/ `4` 质检不通过 / `5` fail-closed 中止。

## 五、任务卡派发流程（本项目工作方式）

> **新会话入口**：`docs/tasks/01-接手指南-新会话入口.md`（现状 / 纪律 / 下一批卡 / 开场步骤）——接手先读它。

1. **我（策划）**：按层拆任务卡 → `docs/tasks/NN-*.md`（模板见 `docs/tasks/_模板.md`），写明：目标 / 产物路径 / **文件白名单** / 禁止事项 / 验收标准 / 回滚方式 / 依赖。
2. **执行（两种派发路径，优先第一条）**：
   - **首选（2026-09-17 起）：ZCode 子智能体 `vox-card-executor`** —— 跑在 ZCode 的「商汤」provider 上（`sensenova-6.8-flash-lite`，`thoughtLevel: high`），自带 `Write/Edit/Bash` 直接落地代码，不经 opencode CLI。它的定义在 `~/.zcode/agents/vox-card-executor.md`（三项拦截检查、只回事实、不提交、不做达标判定的纪律都写在里面）。
     **注意**：ZCode 的子智能体注册表在**会话启动时快照**，新建/改动 agent 定义后需**新开一个会话**才会出现在可用列表里。
   - **回落：`blackiron-opencode-writer` 子智能体 → `opencode run -m sense-nova/sensenova-6.8-flash-lite`** —— `vox-card-executor` 未注册或报错时用它；免费档（`opencode/*-free`）仅在主模型不可用时再回落，且须在报告里点名。
   两种路径都遵守同一条边界：**只回事实，不做达标判定**（判定归验收方）。
3. **验收（我）**：逐条对验收标准 + 本层 AGENTS.md；必要时调只读审查员（`blackiron-code-reviewer` / `blackiron-rule-auditor` / `blackiron-silent-failure-hunter`）做源码级审计。
4. **不达标**：卡退回，不允许"差不多过"。

**敏感数据红线**：本仓是公开预备仓，**任何包含用户录音、真实会话、内部地址/token 的任务卡一律不得派发**——免费档有数据留存风险，付费档同样是外发，条件相同。
