# 13 · 未完成清单（单一事实源）

> **本文件是"还有什么没做完"的唯一权威来源。** 2026-09-19 盘查，基线 `d057bcd`；
> **同日第九批收尾**（T12 线 + 前置包小卡 11 张验收通过，基线 `c301c9e`）；
> **同日第十批：T15 方言链路验收通过**（labs/wubench-dialect，全层九根实测 **1,089** 条全绿
> ——接手指南旧拆分 1,063 少数了 T14c 的 +26 条，adapters 真值 **311** / eval 真值 **208**，详见 00-索引 第十批）；
> **同日 8 条待拍板全部拍板**（用户 2026-09-19 逐条过）：License=**Apache-2.0**、T13 方向=**只准流程决定话轮**、
> kefu 改仓=**允许最小侵入**、state_trigger=**升独立扩展区 `trigger/`**、冻结区=**本批不开**、
> 预算缺省=**维持 60 字**、日频砍因=**记不清（按后期重建设计时再定）**、响度归一=**暂不引入**。
> 拍板明细见 §八 第 12–18 条。
> 其他地方（`00-索引` 的跨批待办、`docs/12` 的任务清单）保留各自语境，但**新增/关闭待办一律改这里**，
> 避免三处各记一份而互相漂移——本项目已经因为"同一件事记在多处"栽过（见 §五 第 8 条）。
>
> **读法**：§二 已清空（2026-09-19 全部拍板）；§三/§四 是可开工的；§五 是代码与文档治理；§六 是对外交付缺口；
> **§八 是已经关闭的**——防止新会话重新捡起已经决定过的事。
>
> **2026-09-21 更新（第十九批）**：**§四#7 关闭**（14 条多分句整段 → T33 落地，
> **不动冻结区**：拆 key + 查询面序列命中）；**§五#20 新增**（包测试 yaml 缺省路径漂移，低）；
> **§三 4 卡不变**。本批基线见 `docs/tasks/00-索引.md` 第十九批节。
>
> **2026-09-23 全量盘点（第二十五批后）**：**§五 五条关闭**——`#1` 由 T27 关（热路径索引化）、
> `#15 / #16 / #17 / #18` 四条由第二十二批「冻结区治理四项」关。**这四条此前一直挂在「随 §三」，
> 实际早已做完**——本次**按代码逐条复核**才划掉，依据：
> `cli/commands/bench.py:204` gate 失败返回 `EXIT_QUALITY`（#15）、`tools/structure_budget/` 台账+检查器在位（#16）、
> `eval/_io.py` 已被 bench/readback/report/duplex_quality 四个模块 import（#17）、
> `core/__init__.py:60` 导出 `METRIC_FIELDS` 且 `cli/commands/bench.py:43` 走 `eval.bench` 公开导出（#18）。
> **§六 #2/#3/#5/#6 关闭**（T19/T20、T23、T24、`docs/19` 均已交付），#4 盲测自然度转为「素材就绪、等真人听测」。
> **当前基线：十一根 1,547 条全绿、0 失败**（2026-09-23 T39 清掉 §五#22 的最后 4 条红后，本仓近期首次全绿）。
> **同批新增（第二十六批）**：**T36 关闭台账的机器绝对路径泄漏**（发布树红线，见 §八#20）；
> **§八 由 19 条增至 20 条**，§五 计数不变（该泄漏未被登记为 §五 待办，属本批发现并当场关闭）。
> **教训（值得记）**：清单条目「做完了没人划掉」与「没做被当成做完了」同样危险——
> 前者会让下一个人重做，后者会让红线失守。**关闭一条必须给出代码级证据位置，不接受"提交信息说做了"。**

---

## 一、一页速览

| 组 | 条数 | 性质 | 谁能动 |
|---|---|---|---|
| §二 待拍板 | **0 条（已清空）** | 原 7 条 2026-09-19 全部拍板 → §八 第 12–18 条 | — |
| §三 冻结区批次 | **4 张卡全部完成 ✅（2026-09-21）** | T27 索引化 / T19 开口策略 / T18 TTL / T20 双工指标——逐卡验收通过（见 `00-索引` 第二十/二十一批） | — |
| §四 扩展区/其他 | **7 项全部完成 ✅**（含 T14 的"响度归一"按拍板暂不引入） | 不碰内核 | — |
| §五 代码与文档治理 | **26 项**（**16 项已关 ✅**；余 10 项：#4 与 #24 中，其余低；**#24 的 ④ 在冻结区、需开批**） | 卫生与一致性 | 可自主推进（冻结区除外） |
| §六 对外交付缺口 | **6 项**（**4 项已交付**：双工 T19/T20、回流闭环 T23、端到端对照臂 T24、里程碑修订 `docs/19`；**1 项半程**：五项指标 4/5；**1 项未做**：盲测自然度——素材与打分表已就绪，等真人听测） | 对照立项案的要求 | 盲测类需真人，其余可自主 |
| §七 外部阻塞 | **2 项**（均已完成/解除，余 1 项未决：验收集 B） | 环境/数据不在我们手里 | 等条件 |
| §八 已关闭 | **22 条** | 别重开 | — |

> **τ_evidence 声明（v15.3 条2）**：本项目预算=业余时间、无资金预算；轮数上限不设硬值，以批次节奏代偿；终止判据=M3 完成（回流闭环+盲测自然度+端到端对照臂）。
> **Adeq 资源声明（v15.3 条7）**：本项目资源=人力（用户 + AI 协作，无雇佣成本）、算力（本机开发机 + 本地/平台模型免费额度）、数据（公开语料 + 自造语料，来源见 docs/05、docs/11）；计量口径=批次节奏（每批=一轮迭代），截至 2026-09-22 全部批次均在资源内完成、无触顶记录；本行为 Adeq_{d,p} 分量的证据工件声明（资源报告）。

---

## 二、待拍板（**0 条——2026-09-19 全部拍板完毕**，明细见 §八 第 12–18 条）

| # | 事项 | 拍板结果（2026-09-19） | 落地动作 |
|---|---|---|---|
| 1 | License | **Apache-2.0** | ✅ LICENSE 已入仓根，README 已改 |
| 2 | 哪些话允许"固定" | **只准流程决定话轮**（问候/确认身份/告别/播报类 + 业务流程可枚举的 key；禁止从用户话术反推 key） | → T13 卡按此出判据（下一批） |
| 3 | 是否允许改 kefu-agent 仓库 | **允许最小侵入**（可选钩子、默认关闭、可摘除） | → 内嵌旁路卡排队（需先侦察 kefu 结构） |
| 4 | `state_trigger` 归属 | **升独立扩展区 `trigger/`**（自带七项 AGENTS.md） | → **T28 迁移卡（本批派发）** |
| 5 | 冻结区批次 | **本批不开**（先消化 trigger/ 迁移与 T13） | §三 挂起，开批时机待定 |
| 6 | 单轮播报预算缺省值 | **维持 60 字** | ✅ 无代码改动，即日生效 |
| 7 | 日频播报为何砍掉 | **记不清了** → 后期重建设计时再定，设计输入带「当初砍过」事实 | ✅ 已带进 §八#17 |

---

## 三、冻结区批次（4 张卡，**2026-09-21 开批并同日全部完成** ✅）

> **2026-09-21 开批**：用户拍板「可以执行 / 逐步开发一步步验证 / 可以去尝试验证」——
> 冻结区批次**正式开批**（本批开 T27；T19/T18/T20 按建议顺序逐步推进，每张验收后再开下一张）；
> kefu 钩子接序列档（T34）与 guided decoding 勘察同期放行。
> D9 纪律不放松：**每张卡都要版本号口径 + 迁移说明**；T27 的裁定 = 语义不变（包格式/API 契约/
> 失败条件/返回值不动）→ **不动 protocol_version、无产物迁移**，在 `assets/AGENTS.md` 记实现变更记录。
> 建议顺序不变：**T27 → T19 → T18 → T20**。理由见下。
> **顺序注（2026-09-19）**：市场视角的重排见 `docs/16 §五`（对外叙事则「14 条多分句拼接」与 golden B 升优先）；开批时两表对照取交集。
> **2026-09-21 更新**：`docs/16 §五` 的第一优先项「**14 条多分句整段→plan 拼接**」**已由 T33 落地，且不必开冻结区**
> ——落地形态 = `docs/06 §6.1.4`「长话术一律拆 key」（拆句铸入，包侧）+ 查询面「逐字覆盖」序列命中
> （`docs/10 §10.7`，扩展区 `adapters/framework_kefu`）；`compiler`/`runtime` 一行未动。

| 卡 | 层 | 目标 | 建议顺序的理由 |
|---|---|---|---|
| **T27** | `assets`〔冻〕 | **热路径索引化**：`pack.lookup` 现在是**逐条线性遍历** + 每次命中**重算指纹** + 每次一次 **`stat`**（`assets/pack.py:110-143`） | **最先**：纯实现优化、**语义不变**、风险最低、且是"量大以后"的前提。验收含"10k 条资产下不劣化"的实测耗时 —— ✅ **已验收通过（2026-09-21，T27d 收口）**：O(1) 索引 + 指纹预计算；热路径 0.5–0.8 µs（旧 61–2115 µs）；契约收口见 `00-索引` 第二十批 |
| **T19** | `runtime`〔冻〕 | **开口策略**：`patience_ms`/`backchannel`/`barge_in`/`rate_band` 从"只校验"变成**被消费** | **次之**：项目名字的另一半，且在前置包场景**可测**（发起方是系统自己，场景可自造；客服场景测不了因为那是别人的行为） |
| **T18** | `runtime`〔冻〕 | **TTL / 失效**（立项案 §4.2 的 `ttl`/`invalid_at`） | 可缓：但在**前置包场景是刚需**——播报过期事实比不播更糟 |
| **T20** | `eval`〔冻〕 | **双工质量指标**（打断准确率 / 假阳性 / 轮转延迟） | 可缓：依赖 T19 |

---

## 四、扩展区 / 其他可自主推进（7 项，**全部完成 ✅**）

| # | 项 | 状态 |
|---|---|---|
| 1 | ~~**`T12` 语料自给管道固化**~~ | ✅ **本批完成**：`tools/corpus_fetch/` 三件套 + 75 条测试入仓（`c68a859`）；回归三数字逐位一致。静默失败审计查出 2 阻塞 + 3 P1 + 6 P2 → 已由 T12b/T12c 全部修复（`0ce8cae`/后续） |
| 2 | ~~**`T16b` 兜底位置校验**~~ | ✅ **本批完成**：兜底（`when: null`）必须最后一条，装载期强制（`b02df7b`）；同族新缺口「`when` 字段必须已声明」由 T16c 一并关上（提交见 00-索引）。类型相容性校验仍缺 → 转本文件 §五 第 10 条 |
| 3 | ~~**跨批待办 3：前置包的 key 可达性检查**~~ | ✅ **本批完成**：`check_reachability()` 报告类 API（T16d，demo 基准实测钉住：未引用 key 2 个 `offer_help`/`ticket_status`、未引用字段 1 个 `ticket_id`）。**更正本行两处过期表述**：原记「3 个 key 只被 script.json 用」实为 **2 个**（`due_tonight` 已被 trigger 引用）；「`overdue_days` 无任何规则引用」**不实**——它被 `overdue` 规则的 slots 引用，真正零引用的是 `ticket_id`。CLI 接线口子 → §五 第 11 条 |
| 4 | ~~**`T22` 结论项 → 话术板派生规范**~~ | ✅ **判定为重复，已关**：trigger.json 本身就是「状态 → 该说什么」的可 Review 映射文件（T16/T17 已交付格式 + 行为 + 预算）。裁定详见 §八 第 10 条；`docs/12` T22 行已标注 |
| 5 | ~~**`_rule_matches` 暴露为公开 API**~~ | ✅ **本批完成**：更名公开 `rule_matches`（AST 级语义零变化），labs 覆盖表迁移完毕（148 断言不变），5 处旧名说明文字由 T17d 清零 |
| 6 | **`T14` 响度归一 + 回读 CER** | **部分完成**：回读 CER 闭环 ✅（T14：`adapters/asr_omlx` + `eval/cer.py` + `eval/readback.py`；真机闭环 3 条 fin-cs 话术 CER 全 0.0；模块入口假成功缺陷 T14b 已修）。**响度归一：2026-09-19 拍板「暂不引入」**——维持 `docs/06` 推迟条款：现在包内话术出自同一 TTS 引擎、响度天然一致，等多引擎混播或真实听感反馈时再开卡（见 §八#18） |
| 7 | ~~**14 条多分句「整段→plan 拼接」**（`docs/16 §五` 首推项）~~ | ✅ **已完成（T33，2026-09-21）**：`packs/heat_kefu` 拆句铸入 29 条（40→69 key）+ `find_hit_sequence`（`docs/10 §10.7` 逐字覆盖）+ `labs/heat-kefu-seq/` 四臂实测（14/14 命中、命中臂 P50 ≈ 5 ms vs 慢路 ≈ 575 ms、零 TTS）。**不动冻结区**（拆 key 是 `docs/06 §6.1.4` 的既定形态）。真链路接入（kefu 钩子多段拼接）另开跨仓卡 |

---

## 五、代码与文档治理（26 项：**16 项已关 ✅ / 10 项开放**，用户特别关心的部分）

| # | 项 | 证据 / 位置 | 严重度 |
|---|---|---|---|
| 1 | ~~**`pack.lookup` 热路径三件事**：线性遍历、每轮重算指纹、每轮 `stat`~~ ✅ **已关（T27，2026-09-21）**：O(1) 索引 + 指纹预计算 + 文件存在性移出热路径（10k 条下热路径 **0.5–0.8 µs**，旧 61–2115 µs）；契约收口见 `assets/AGENTS.md §⑧` 与 `00-索引` 第二十批 | `assets/pack.py:110-143` | 已关 |
| 2 | ~~**`assets/tests/test_pack.py` 的 `mkdtemp()` 不清理**~~ | ✅ **本批完成**（T04d）：两处均挂 `addCleanup`；对照实验实测修前 +18 泄漏目录/两遍 → 修后 **0**（`3ba6966`） | 已关 |
| 3 | ~~**`format.py` 超行** vs `adapters/AGENTS.md §②` 的 ≤150 行~~ | ✅ **已随 T28 迁移解决（2026-09-19）**：`adapters/state_trigger/` 升独立扩展区 `trigger/`，`trigger/AGENTS.md §②` 按同类大文件口径登记（不适用 ≤150 行量化标准，同 `compiler/source.py` 312 行），「架构错位」不再存在；**拆分留口子**——若未来要拆 `format.py`（945 行），按新层变更纪律另卡 | 已关 |
| 4 | **敏感判定是按字段名嗅探的 lint，不是保证** | `docs/12 §12.10`。名叫 `order_ref`、装着客户标识的字段被声明成 `structured` 也不会报错。更强判据（allowlist 化 / 值模式识别）**登记为候选**，因涉及误报权衡要业务侧参与 | 中 |
| 5 | **`packs/demo-brief` 的 `days_left` 声明 `min:0`** 与 `overdue` 的语义（到期日之前）张力 | 导致三个派生字段只能**透传源值、不能计算**；demo 数据仍在（T21/T21b 用 fixture 绕开） | 低（demo 数据问题） |
| 6 | ~~**`packs/demo-brief` 的 key/字段可达性**~~ | ✅ = §四 第 3 条，T16d 已交付检查 API；实测：未引用 key 2 个、未引用字段 1 个（`ticket_id`，敏感层，属可接受的声明冗余） | 已关 |
| 7 | ~~**`README.md` 首屏过期**~~ | ✅ **本批完成**：Status 行改为实际状态（九测试根 1,063 条、真链路实测、3 个业务包、回读 CER 可复现），「一句话」按 `docs/11` 实测边界收紧措辞，文档索引补齐 06–13 | 已关 |
| 8 | ✅ **已修**：`00-索引` 原跨批待办表 ①② 仍标"待处理"，实际**已由 T06 结项**（裁定在 `docs/07 §7.5/§7.6`） | 本次盘查发现并改正 | — |
| 9 | **旧文档的坐标漂移**：`docs/12` 早期版本把触发器落在 `runtime`、T21 定为"业务状态适配器"，都已改；新会话读旧笔记可能对不上 | 各处已更正，但历史会话记录里仍是旧的 | 低（读过期笔记的风险） |
| 10 | **`when` 期望值与声明 type 的类型相容性校验缺**（如对 bool 字段写 `{"gt": 5}`）——T16c 明确不做并用 `test_when_type_mismatch_not_checked` 锁定当前状态；改语义时**必须同步改该测试**（测试已随 T28 迁至 `trigger/tests/`） | `trigger/format.py`（原 adapters/state_trigger/） | 低 |
| 11 | **`check_reachability` 未接 CLI**（`vox verify` / `pack check` 均不消费它）——库 API 已有、无人调用；接线落 cli/（冻结区），须与 §三 冻结区批次同批考虑（API 本体现居 `trigger/reachability.py`） | `trigger/reachability.py` | 低 |
| 12 | ~~**labs 的 `semantic_probe*.py` / `cross_check_template_rate.py` 仍是无测试的一次性脚本**——1.8% / 0.2% / 语义档 11.8% 这些对外数字的出处没有回归保护~~ | ✅ **已收（第十四批补记，2026-09-19）**：四个对外数字**全部逐位复现**（验收方亲跑，1.7978% / 0.24% / 11.8% / 29.5%）；对账表（命令+期望值+实测）落 `labs/corpus-harvest/README.md §十`。**裁定不固化脚本进 tools/**——语义路线已实测否掉（§八#1），脚本保留 labs 原样作历史证据链；回归保护以「对账表 + 命令」形态存证 | 已关 |
| 13 | **`resolve_asr` 对 ASR 构造器不收的 kwargs 静默丢弃**（替身场景 ergonomic 取舍，docstring 已声明）——若要 fail-closed 需改抛错，牵动替身用例 | `eval/readback.py` | 低 |
| 14 | **准入判据的机器化 checker 未落**（2026-09-19 T13 留口子）：判据已成文（`docs/14`），申报制现阶段靠 `admission.md` + 验收方抽查；机器防线 = `pack.json` 增 `admission` 字段（key→类别）+ checker，**落点开放**——候选是冻结区批次的 `vox pack check` 接线（cli/ 冻结区），扩展区无合适宿主层 | `docs/14 §四` | 低 |
| 15 | **R06 评测门槛没有牙齿**（规律卡审计）：~~`cli/commands/bench.py` gate 失败仍 exit 0~~ ✅ **门禁已关（T21「冻结区治理四项」，2026-09-21）**——`cli/commands/bench.py:204` 在 `reference_gate.passed` 为假时返回 `EXIT_QUALITY`（文件头 `:21` 有口径说明）。**残余仍开放（冻结区）**：`eval/` 不消费 fin-cs 的 2433 条 golden——2026-09-23 复核实测 `eval/*.py` 对 `golden` / `corpus_derived` **零引用**（bench 必须显式 `--corpus`，无缺省），2433 条对照集至今只有 `labs/` 的一次性基线脚本在消费 | 规律卡审计 2026-09-19 → 2026-09-23 复核 | 门禁已关；残余低 |
| 16 | ~~**R09 结构预算**（规律卡审计）~~ ✅ **已关**：fetch 成功数下限断言由 **T32** 修（全跳过缺省 rc 3 + `--allow-all-skipped` 显式放行留痕，负例实测）；「无适应度函数 / 无豁免台账」两半由 **T21「冻结区治理四项」** 落地——`tools/structure_budget/{check.py,LEDGER.md,snapshot.json}` 在位，**2026-09-23 T35 验收即用 `check.py` 判定新适配器 146 可执行行 ≤150**，机器可读阈值行在 `adapters/AGENTS.md §⑩` | 规律卡审计 2026-09-19 → 2026-09-23 复核 | 已关 |
| 17 | ~~**eval 三份逐字拷贝 helper + append-only 生长**（结构审计）~~ ✅ **已关（T21「冻结区治理四项」）**：`eval/_io.py` 抽成标准库级收敛点，**2026-09-23 复核实测已被 `bench` / `readback` / `report` / `duplex_quality` 四个模块 import**；写指纹↔验指纹共用同一实现的「咬合耦合」与原 WHY 论证（为何是文件字节摘要而非语义摘要）全部留在 `_io.py` 的 docstring 里。**留口子不变**：bench 再进一批就拆 `_arm` 聚合与 CLI 壳 | 结构审计 2026-09-19 → 2026-09-23 复核 | 已关 |
| 18 | ~~**公开面绕行卫生**（结构审计）~~ ✅ **已关**：扩展区（trigger ×5 / framework_kefu ×4 内部路径）由 T32 修（生产代码 0 命中，测试目录豁免）；**剩余两条冻结区项也已由 T21 补齐**——2026-09-23 复核：`core/__init__.py:30,60` 已把 `METRIC_FIELDS` 放进 `core.__all__`；`cli/commands/bench.py:43` 改为 `from eval.bench import ... load_corpus`（走公开导出，本模块只做 re-export） | 结构审计 2026-09-19 → 2026-09-23 复核 | 已关 |
| 19 | **adapters 三文件超行未登记**（结构+交付审计同发现）：bridge.py 可执行 231 / kefu_client.py 236 / asr_omlx 176 超 §② 的 150 且无豁免——**已补账**：`adapters/AGENTS.md §⑨` 登记豁免+拆分留口子（asr_omlx 拆传输/协议两层可自然回 150 内） | 已关（2026-09-19 §⑨ 补登记） | 已关 |
| 20 | **包测试的 yaml 默认路径漂移**（2026-09-21 T33 验收发现）：`packs/heat_kefu/tests/test_source_of_truth.py` 的 `YAML_PATH` 缺省拼的是 `<repo>/../kefu-agent`（= `<repo>/../kefu-agent`，不存在；实际 kefu 仓在本机另一目录，非本仓相对路径）→ **不设 `KEFU_HEAT_YAML` 时本机也走 24 条 skip**（T31b 起就是如此，"诚实 skip"没错但**默认值形同虚设**）。修法：缺省探测多个候选路径（`../kefu-agent` 与 `../../kefu-agent`），都无则仍 skip；或把候选写进 `admission.md` 的复现命令 | T33 验收发现 | 低 |
| 21 | ~~**构建产物未进 `.gitignore`**~~ ✅ **已关（T33b，2026-09-21）**：`.gitignore` 加 `packs/*_build/`（`git check-ignore -v` 实测有输出）；另 CI 加构建步（`packs` 的真包断言在 CI 生效） | T33 验收发现 → T33b 修复 | 已关 |
| 22 | ~~**T18 遗留：两处 TTL 用例与墙上时钟绑定 + `executor._diagnose` 未串注入 `now`**~~ ✅ **已关（T39，2026-09-23，用户开批）**：① `assets/tests/test_ttl.py` 的「失效前能取到」断言走了默认 `now`（=墙钟）→ 写死的 `2026-09-22T13:00:00Z` 被墙钟越过后必然转红；② `runtime/tests/test_executor_expiry.py` 的 `TestFutureExpiryPlays` 夹具把失效时刻写死为 `2026-09-23T00:00:00Z`，而该类有两条用例用默认 now（=墙钟）→ **日历炸弹**；③ **真产品缺陷**：`runtime/executor.py` 的 `_diagnose` 调 `pack.lookup` **未串 `self.now`**，与 `_decide_unit`:441 的 `_expired_at(...)`（用 `self.now`）**判据源不一致** → 注入的 now 未过期、墙上时钟已过期时 lookup 返回 None，撞上「判定不一致」fail-closed 抛 `AssetPackError`。处置：① 注入 `now=EXPIRES_BEFORE`；② 夹具改**相对墙钟**构造（+1h），需要精确时刻的用例仍显式注入；③ `_diagnose` 的 lookup 补 `now=self.now`（判据回到唯一来源）。**注入验证**：把 ③ 改回吃墙钟 → `test_expiry_is_now_dependent_not_wall_clock` 判红。修复后**十一根 1,547 全绿、0 失败**（本仓近期首次），公开 CI 随新快照转绿 | 收口批复算 2026-09-22 发现；2026-09-23 用户开批并修复 | 已关（冻结区） |
| 23 | **神经 TTS 接入的两处已知问题**（2026-09-23 实测，T35 登记，均未修）：① `Qwen3-TTS-12Hz-1.7B-Base-8bit` 档**产出退化**——同参考同参数下 ASR 回读乱码、且把 `ref_text` 内容混入输出，中位 F0 272.7 Hz（参考原声 192.0 Hz）；② `/v1/audio/speech` 的**流式产物是无界 WAV**（`RIFF…ffffffff`）——`ffprobe`/播放器可读，`runtime.audio.read_wav` 会拒（适配器暂只走非流式）；③ **manifest/ledger 的 `voice`/`model_version` 取包源声明**（`compiler/prebake.py:416-417`），适配器级 voice 覆盖只参与增量复用判定（`:259-260`）→ 换引擎后包身份标注会失真（本地克隆包实测仍记 `voice=Tingting`/`model_version=macos-say`）；④ 克隆档产物**首字偶发多一个虚字**（抽检 3 条中 1 条：`clarify_repair__2` 原文「家里不热、漏水还是其他问题？」回读「瓶家里不热，漏水，还是其他问题？」）——引擎侧特性、不影响可懂度，预铸质检（电平/静音）不覆盖内容层；⑤ ~~`tts_omlx` 测试在无 ffmpeg 的机器（CI runner）上环境假设错误~~ ✅ **已关（T38，2026-09-23）**：`FFMPEG_BIN` 回落裸名导致四条替身用例在 CI 上 **2 条 ERROR + 2 条假绿**（负例因「ffmpeg 不存在」抛了同一个 `TtsError` 而通过）——已改为占位可执行文件解析 + 类级 skipUnless 兜底，且在最小 PATH 下实测 4 条真跑。详见 `docs/tasks/T38-*.md`。另记：克隆档每次多花约 5 秒（参考条件化），0.6B 档不带参考 1.2× 实时、带参考 ~0.3× 实时；本机 24 GB 内存下 oMLX 多模型互相挤出会带来 ~68 s 重载。复现与数字见 `labs/tts-clone-probe/README.md` §二/§五 | T35（2026-09-23） | 中（①②扩展区；③ 冻结区） |

---

| 24 | **新人跑不起来的四处缺口**（2026-09-23 以「刚 clone 的开发者」身份实跑一遍后登记）：① ~~没有一份对外教程~~ ✅ **已补（`docs/22-五分钟跑起来`，贴实机真值输出）**；② ~~README Quick Start 没一步能听见声音~~ ✅ **已改**（现在是 铸包→播一句→听 → 且标了两个前提）；③ ~~**无示例 plan / 示例语料**~~ ✅ **已关（T42，2026-09-23）**——`examples/plan.json`、`examples/bench-corpus.json`、`examples/README.md` 三文件落成真文件；干净暂存目录实跑 `pack build` → `run examples/plan.json` → `bench … --corpus examples/bench-corpus.json` 三条 **rc 依次为 0 / 0 / 0**（`hit=4 miss=0 tts_calls=0`、`observed_hit_rate=1.0` 门禁通过）；`docs/22 §5` 两行随之翻 ✅ 并指向真文件；④ **无列 key 的命令**——新人想知道包里有 69 个什么 key，只能手翻 `manifest.json`；`vox pack list` 之类的落点在 `cli/`（**冻结区**），需开批；⑤ ~~非 macOS 走不通且 README 没写~~ ✅ **已在 README 与 `docs/22 §1` 标明**（缺省引擎是 macOS `say`，换 `--adapter`）。**根因（值得记）**：本仓的「读者」一直是 **AI 执行器与验收方**——18 篇 docs 全是设计/口径/合规/逐卡验收，**零教程**。| 2026-09-23 实跑盘点 | 中（仅剩 ④） |

| 25 | ~~**模型引擎铸整包会随机失败**（T40 验收实测，2026-09-23）——阻塞「Linux 上用模型 TTS」这条方向~~ ✅ **已关（T41，2026-09-23）**：落地「归一失败的有界重试」——**只重试随机类**（产物归一自检不过）、确定性失败（空文本/未知语速档/非 2xx/响应过大/缺 ffmpeg）一次都不重试；沿用既有 `MAX_RETRIES=5` / `RETRY_DELAYS`，**每次重试写 stderr 留痕**（固定格式，门禁按「重试」计数），耗尽仍 fail-closed（末次错误带重试次数）。**形态取 A（拆文件）**：按 §⑨.1 对 asr_omlx 的同一思路，把「HTTP 传输 / 请求体 / 传输层重试」拆到 `adapters/tts_omlx/transport.py`，`adapter.py` 只做编排并回到 150 行内（实测 115 可执行行，transport.py 71）——**未新增豁免**。**实测**：连铸三次 `packs/demo-brief`（48 条）整包 **全部 rc=0**，各次重试 **6 / 6 / 10** 次。**如实记账**：这**不是"完全稳了"**——引擎仍是**采样型**（T40 实测同文本 8 次输出 sha256 全不同），整包成功靠有界重试吸收随机失败；随机类失败仍会照常发生，只是被吃掉。前提不变：`--adapter` 显式给 + 端点在线 + `ffmpeg`。**顺带记（未修）**：归一自检的报错一律说成「模型头静音超限」，但复核员实测 `_trim` 裁到 5 ms 的边缘样本会在 `_peak_normalize`（增益 < 1）后掉回绝对门限——报错归因可能误导排查方向（既有代码） | T40 验收（2026-09-23）→ T41 修复（2026-09-23） | 已关 |

| 26 | ~~**adapters 测试套件被 T41 拆分拖慢约 4 倍**~~ ✅ **已关（T41 收尾批，2026-09-23）**：**首判有误，实测改判**——真因不是夹具漏 patch 传输层 sleep，而是**重试判定过宽**：postprocess 对**所有**归一内失败统一包「产物归一失败: 」前缀（ffmpeg 退出码非零 / 产物不是合法 WAV / 全静音等**确定性**失败也带它），`is_normalization_failure` 单认前缀 → 确定性失败也进重采循环、5 次真实退避（270s 增量 ≈ 3 条确定性用例 × 67s）。**这不只拖慢测试**：真铸包遇到坏 ffmpeg 会每条空转 67 秒。**修法**：判据收紧为双条件「产物归一失败 + 静音仍超限」（`transport.is_normalization_failure`），分类测试补 4 条确定性包装反例，夹具同时归零 `OmlxTtsTransport._sleep`（防连接类用例真退避）。实测 3.14 全量 48 用例 **270s → 2.4s**、3.12 → **1.7s**（套件余量波动来自真端点冒烟，属引擎时序非代码） | `adapters/tts_omlx/transport.py`（`is_normalization_failure`）· `tests/test_adapter.py`（分类反例 + 夹具） | 已关 |

## 六、对外交付缺口（对照立项案的要求）

> 结论一句话（2026-09-23 复核后重写）：**M0/M1/M2 已交付，M3 三项已交 2；立项 §五 五项指标 4/5 有数据。**
> 唯一没交的两件是 **① 盲测自然度**（素材与打分表已就绪，等真人听测——AI 不能代填）与
> **② 单位会话成本的直接口径**（现有"合成字符数"只是代理量）。

| # | 立项要求 | 现状 |
|---|---|---|
| 1 | §五 五项指标 | **4/5 有数据或半程**：首响延迟 ✅、命中率/时长占比 ✅、**双工质量 ✅（T20：打断准确率/假阳性/轮转延迟，固定种子可复现）**；**拼接自然度**半程——回读 CER ✅ 可复现（真机闭环 CER 0.0），**盲测 A/B 素材已就绪、等真人填表**（见 #4）；**单位会话成本** ⚠️ 只有"合成字符数"代理量 |
| 2 | §二 第 4 条「双工行为控制器」 | ✅ **已交付（T19）**：四参数各有可观测行为（等待窗口/背景回应/可打断标记/关键信息强制 slow）+ `policy_stream` 事件流 + 音频不变性实测 |
| 3 | M3「回流闭环（日志挖掘 → 字模建议）」 | ✅ **已交付（T23，2026-09-22）**：`tools/feedback_mining/`——从轮级留痕+事件流挖候选话术 → 形式条款机器化（B1–B5）+ A1/A2 标注 `pending_human` → 建议清单对接 `docs/14` 申报；拒收留痕（B1/B2/B4 各实测）；确定性亲验 |
| 4 | M3「盲测自然度报告」 | ⏳ **素材就绪、等真人听测（2026-09-23）**：`labs/naturalness-ab/`——三臂（①预铸拼接 / ②运行时逐段合成拼接 / ③一次性整句合成）+ 固定种子盲化（c1/c2/c3 洗牌）+ 60 段音频落 `~/vox-naturalness/listen/` + 两张空打分表（`sheet_clips.csv` 60 行 / `sheet_items.csv` 20 行）；**出报告的 `report.py` 已写好**（fail-closed 完整性闸 + MOS 中位 + bootstrap CI + 接缝可察觉率 + 名次 + ①vs② 一致性）。**卡点与 golden 验收集 B 同性质：需要人的耳朵，AI 不得代填** |
| 5 | §九 第 4 条「端到端对照那一臂」 | ✅ **已交付（T24，2026-09-22）**：`labs/e2e-vs-cascade/`——臂 A 命中 0.1ms/逐字 1.0、未命中 4256ms/逐字 0.0；臂 B（云端 qwen-omni 流式 S2S）首字节 717ms、音频 10/10 有效（字节 8 种取值=不保证逐字）；三维对照（延迟/可控性/可审核性）齐 |
| 6 | §八 里程碑需修订 | ✅ **已修订（2026-09-21）**：`docs/19-里程碑修订.md`——M0/M1 完成、M2 以「可审核资产层 + 双工控制器」替代原「语义命中」（后者实测证伪）、TTL 自 M3 前移、端到端对照臂提为 M3 项 |

**另一条不影响交付但必须记住的**：`docs/11` 的裁定 1 **维持不变**——语义检索**没有**升级为"命中"的依据，红线不动。

---

## 七、外部阻塞（2 项，1 项已终 ✅）

| # | 项 | 卡在哪 |
|---|---|---|
| 1 | ~~**`T15` 方言链路**~~ | ✅ **已完成（第十批，2026-09-19）**：`labs/wubench-dialect/`——固定 seed 抽 Wu-Bench 100 条 → oMLX ASR → CER。实测 **CER mean 0.2686 / P50 0.2546 / P95 0.5723 / bootstrap 95% CI（对 P50）[0.2143, 0.3]**（100 条全 ok，可复现命令见其 README；经静默失败审计 1P1+3P2 修复后复验通过）。**范围裁定**：原链路描述里的「提案→命中」不在卡内（语义提案已否 §八#1；方言集无 key 标注不得自造命中 第八批裁定 3）——产出的是**方言域 ASR 能力边界数字**，已进 §八 第 11 条 |
| 2 | **golden set 验收集 B（真人标注）** | 未做。`docs/11 §11.5` 要求它必须是 `labeled_by=human:*`；现在只有半自动的对照集 A（2433 条） |

---

## 八、已经关闭的（**别重开**）

| # | 事项 | 结论 / 出处 |
|---|---|---|
| 1 | **用语义检索让上游按 key 说话** | ❌ **已实测否掉**：top-1 11.8%、策略级对照 12.7%（随机 8.3%），与门槛差 8 倍。**key 只能由业务流程产出**（`docs/11 §11.9.4`、`docs/10 §10.2`） |
| 2 | **拿用户话术去逐字比客服 variant** | 范畴错误——`docs/10 §10.2` 的逐字比对比的是**助手打算说的那句话**（`docs/11 §11.9.3`） |
| 3 | **`when: null` 是否算命中** | ✅ 已裁定：**算**（显式兜底）；原红线收窄为"不存在兜底规则且无规则匹配时才抛错"；**不得由实现自行挑选兜底**（T17b） |
| 4 | **私人/个人数据能否进前置包** | ✅ 已定：**不进**，前置包只做业务型；个人向的"常驻状态式提醒"整块移到后期单独设计 |
| 5 | **主动提醒链路（旧 `heitie-weekly-summary`）** | ✅ **后期再建**，且是**重新设计而非复活**（旧设计有滞后性：等人工晚上注入，交互时本该是常驻状态） |
| 6 | **`docs/12` 早期把触发器放在 `runtime`** | ✅ 已更正：`state → plan` 是"决定说什么"，属**接缝之上**，不属本层（`docs/10 §10.1`）→ 走扩展区 |
| 7 | **跨批待办 ①②**（R-5 的 `reason` 落点 / R-3 的检查口径） | ✅ 已由 T06 结项（`docs/07 §7.5/§7.6`）；跨批待办 ③④ 已由 T07 落地 |
| 8 | **「同名镜像等价」这个假设** | ❌ 已否掉：`MagicHub` 与 `MagicDataTech` 的同名方言集**许可不同**（前者无 front-matter 许可、后者 apache-2.0，两者正文都是 CC BY-NC-ND）→ 一律以正文为准（`labs/corpus-harvest/README.md §三`） |
| 9 | **`T21` 那条"四条规则不可达、必须改包"的诊断** | ❌ **已更正**：真根因只是 fixture 行覆盖不足 + `greeting` 顺序在先；**加行即可，不用改包**（T21b 实测 6/6 可达） |
| 10 | **`T22` 结论项 → 话术板派生规范** | ✅ **判定为被 T16 覆盖（2026-09-19 第九批）**：`docs/12` 对 T22 的定义是"定义『什么状态该说什么话』的可 Review 映射文件"——trigger.json + `format.load_trigger`（校验）+ `trigger.build_plan`（行为）+ 预算闸就是它。**不再单开**；其「从结论项自动派生」的余量属个人线后期设计（私人数据不进前置包，§八 第 4 条）。出处：`docs/12` T22 行标注 + 本批判定 |
| 11 | **T15 的「提案→命中」环节** | ✅ **已完成并收窄（2026-09-19 第十批）**：方言链路实测止于 CER（`labs/wubench-dialect/`），「提案→命中」**永久移出 T15**——语义提案已实测否掉（§八#1：top-1 11.8% vs 门槛 98.7%），方言集没有「该命中哪个 key」的标注、第八批裁定 3 明令不得自造命中。实测数字：吴语 100 条抽样，oMLX Qwen3-ASR 转写 **CER mean 0.2686 / P50 0.2546 / P95 0.5723 / CI95（对 P50）[0.2143, 0.3]**——**任何靠 ASR 文本做判定/检索的下游都要按 ~25–27% 的字符错误基底来估**。出处：`labs/wubench-dialect/README.md` + 任务卡 `docs/tasks/T15-labs-方言链路实测-WuBench吴语ASR-CER.md` 验收记录 |
| 12 | **License 选哪个** | ✅ **Apache-2.0（2026-09-19 拍板）**：LICENSE 已入仓根，README 已改；与仓内 MIT/Apache-2.0 派生语料兼容（台账见 `labs/corpus-harvest/`）；`packs/*/golden/` 不进公开分发不变 |
| 13 | **哪些话允许"固定"（T13 方向）** | ✅ **只准流程决定话轮（2026-09-19 拍板），判据已成文（同日 T13 完成）**：`docs/14-预铸准入判据.md`——准 A1 流程决定性话轮 / A2 业务可枚举播报（须过 B1–B5 形式条款），禁 ⅰ 用户话术反推 / ⅱ 语义模糊类 / ⅲ 语料模板复用 / ⅳ 高频主张；验收按 `admission.md` 申报制 + 验收方抽查；packs/AGENTS.md §③ 已加第 6 条。**机器化 checker 落点开放** → §五 第 14 条 |
| 14 | **是否允许改 kefu-agent 仓库** | ✅ **允许最小侵入（2026-09-19 拍板），同日落地（第十四批 T29/T30）**：kefu `synthesize` 接缝钩子已交付——默认关（4 环境变量）、引擎不一致永久禁用、未命中原路留痕、可 21 行摘除；kefu 既有 40 测试全绿零行为差。**brain 按 key 说话（真命中率的前提）已由 `docs/15` 方案 A′ 落地**（2026-09-23 更正：原文写「仍未开卡」，**已过期**）——落地形态**不是改 brain**，而是勘察发现 brain 流程轮本就 `preset.get()` 逐字取 yaml，于是把 `供热预设.yaml` 铸成**同源包**（T31b `packs/heat_kefu`，指纹门禁），实测流程轮逐字命中、真链路字节级（T34） |
| 15 | **state_trigger 归属** | ✅ **升独立扩展区 `trigger/`（2026-09-19 拍板）**：自带七项 AGENTS.md；依据：state→plan 是「决定说什么」，属接缝之上（docs/10 §10.1），扩展区「随便加」的纪律适合成长期，也解禁第二个同类模块。→ T28 迁移卡（第十批同日派发） |
| 16 | **冻结区批次开批时机** | ✅ **本批不开（2026-09-19 拍板）**：先消化 trigger/ 迁移与 T13；方向已接受，§三 4 卡挂起，开批待定 |
| 17 | **单轮播报预算缺省值 + 日频砍因** | ✅ **缺省维持 60 字（≈12.9 秒 @normal，参数可覆盖）；日频砍因记不清（2026-09-19 拍板）**——后期个人线重建设计时再定，设计输入带「当初砍过日频播报」这个事实（与 §八#5「重新设计而非复活」一致） |
| 18 | **响度归一是否引入音频处理链** | ✅ **暂不引入（2026-09-19 拍板）**：维持 `docs/06` 推迟条款——现在包内话术出自同一 TTS 引擎、响度天然一致；回读 CER 已保证内容正确性。再评估的触发条件：出现多引擎混播，或有真实听感反馈 |
| 19 | **14 条多分句整段的落地形态** | ✅ **T33 已落地（2026-09-21），形态 = 拆句铸入 + 查询面序列命中**：**不采用**「整段铸入」（撞 compiler 一句一 variant 硬校验 → 要动冻结区）、**不采用**「同 key 多 variant」（`variant="auto"` 按稳定散列只取一条 → 会只播半句；且污染"同句多说法"语义）、**不采用**「多分片 `part_index>0`」（`docs/06 §6.1.4` 明确本轮不引入）；**采用** `<源 key>__<句序>` 拆 key（`docs/06 §6.1.4`「长话术一律拆 key」的既定形态）+ `find_hit_sequence` 逐字覆盖（`docs/10 §10.7`）。附带裁定：**段序不做约束**（重排输入仍命中、播出顺序 = 输入顺序；加约束会让"确认句+提示句"混搭输出无法命中）、**同文本段允许命中别名**（`clarify_work_order__2` / `clarify_repair__3`，实测两条资产音频 sha256 相同 → 播放等价）。实测：`labs/heat-kefu-seq/`（14/14、命中臂 P50 ≈ 5 ms vs 慢路 ≈ 575 ms、零 TTS） |
| 20 | **台账记本机绝对路径（发布树红线）** | ✅ **已关（T36，2026-09-23）**：`labs/corpus-harvest/corpus.lock.json` 的 25 处路径值是**外部卷的机器绝对路径**——**下次推送就会直接带出去**（发布树六类扫描红线）。根因不是记错，而是「台账要求绝对路径」这条**口径本身**有问题：既泄漏盘位布局、又对别的机器不成立（收口批正是照该口径把它从 `~/corpus/…` 改成绝对路径的）。处置：改为 `${CORPUS_ROOT}/<相对>` 锚定 + 两份生成器同改（防重长）+ 台账就地迁移 + `docs/11 §11.3` 口径同步 + `--recheck` 按同一环境变量还原（**指错根报缺失、退出码 1，不静默**）。**附带裁定**：盘位/家目录前缀的**字面写法本身也不写进仓**（写下来等于让扫描永远非零，且每次都要重新裁定「那是路径还是检查项的名字」）——类别口径以 `docs/18 §一` 为唯一定义处。卡与验收记录：`docs/tasks/T36-tools-语料台账路径可移植化.md`；批次记录见 `docs/tasks/00-索引.md` 第二十六批 |
| 21 | **分发排除只是文档里的人工指令（公开仓现存违规）** | ✅ **已关（T37，2026-09-23）**：`docs/11 §11.7` 的排除清单**两条都被违反且都在公开仓里**——① `packs/fin-cs/golden/corpus_derived.jsonl`（1,192,844 B / 2433 行第三方逐字原文）；② `labs/corpus-harvest/raw/semantic_probe.jsonl`（2433 条 `user_utterance` 原文）。根因：仓库**没有 `.gitattributes`**，导出走 `git archive HEAD` 对跟踪文件一视同仁——「排除」**没有执行者、漏了也不报错**，第一次推送就漏了且此后两轮快照一直带着。更该记账的是 `docs/20` 的合规自审里写着「分发排除**已兑现**」——**在写下时就是不实的**。处置：改为 `.gitattributes` 的 `export-ignore` **机器强制** + 公开仓随新快照纠正 + `docs/20` 更正留痕。**新增口径**：合规记录里不得出现「已兑现」这类无法当场核验的说法——**要么写出核验命令与结果，要么写「未核验」**。卡与验收记录：`docs/tasks/T37-分发排除的机器强制.md` |
| 22 | **`tts_omlx` 测试的 CI 环境依赖与假绿（T35 遗留）** | ✅ **已关（T38，2026-09-23）**：快照 `06f7488b` 推送后 CI 三档全红，其中 2 条红来自 T35 新增的 `adapters/tts_omlx` 测试——`FFMPEG_BIN` 回落到裸名 `"ffmpeg"`，而适配器会先做 `shutil.which` 可用性检查，CI runner 没装 ffmpeg → 抛 `TtsError`。**后果两种都坏**：正向用例 ERROR、**两条负例假绿**（断言「格式不合规/非 0 退出要抛错」，因「ffmpeg 不存在」抛了同一个异常而通过，待验逻辑一行没跑）。本机装了 ffmpeg 故验收时没暴露——**「本机绿≠CI 绿」又一次应验，且这次更隐蔽：不是红，是绿得没意义**。处置：占位可执行文件解析 + 类级 `skipUnless` 兜底；**最小 PATH 下实测 4 条真跑**（不是 skip），注入验证判红。属**我方 T35 验收漏检**，如实记账 |

---

## 九、本清单的维护

- **新增待办**：加进对应小节的表，并在 `docs/12` 或卡里留一条指向这里的指针；
- **关闭待办**：**移进 §八 并写明结论出处**——不要只删掉，删掉会让下一个人重新捡起来；
- **每次盘查后**：更新本文件顶部的日期与基线 commit。
