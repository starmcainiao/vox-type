# 19 · 里程碑修订（2026-09-21）

> 立项案 §八 的原版里程碑写于 **2026-09-16**（开工前）。此后 21 批交付把其中多项完成、
> **一项被实测证伪**。本文件是**修订版**——作为立项案 §八 的对照与替代（对外也用这一版）。

## 一、原版 vs 现状（逐项）

| 里程碑 | 原版内容 | 现状 |
|---|---|---|
| **M0** | 口子定稿（三契约）+ 结构先导图 + 验证 harness 骨架 | ✅ **完成**：三契约见 `docs/02`（plan / 资产注册表 / 双工参数）；harness = `eval/bench.py`（T08） |
| **M1** | 精确命中闭环——字模库 + 精确 key 命中 + 对拍第一组数据 | ✅ **完成**：`compiler → assets → runtime → eval` 闭环；对拍实测快路 P50 **0.232 ms** vs 慢路 515.3 ms（`docs/09`） |
| **M2** | 语义命中 + 槽位模板 + 多语速档 + 双工控制器参数化；**此时带数据开源** | **四项各有结论**：<br>· 语义命中 ❌ **已实测证伪**（2433 条真实语料逐字命中 0.00%、语义档 top-1 **11.8%** vs 门槛 98.7%，`docs/11 §11.9`）<br>· 槽位模板 ✅（T07：槽值现场合成 + 前后微停顿）<br>· 多语速档 ✅（`rates` 机制：slow/normal/fast）<br>· 双工控制器 ✅（T19 四参数被消费 + T20 双工质量指标） |
| **M3** | 回流闭环 + TTL/版本失效 + 盲测自然度报告 | · **TTL/失效 ✅（T18）——但它在 M2 阶段就交付了，故前移**<br>· 回流闭环 ❌ 零实现<br>· 盲测自然度 ❌ 等真人标注（与 golden 验收集 B 同批） |

## 二、修订版里程碑（2026-09-21 起）

### M0 · 口子定稿 ✅
三契约 + 结构图 + harness 骨架——已交付。

### M1 · 精确命中闭环 ✅
字模库 + key 直查 + 快慢路对拍出数——已交付。

### M2 · 可审核资产层 + 双工控制器（**原 M2 的重写**）
> 原 M2 的「语义命中」已证伪 → 替换为项目的**真实差异化**（`docs/16 §一 P3/P4`：
> 别人把缓存当技巧，本仓把它做成**有准入、有质检、有指纹、可审计的层**）。

- ~~语义命中~~ → **明确不做**（红线不动：逐字相等是唯一被允许的判定，`docs/10 §10.2` 裁定 1）
- **预铸准入判据**（`docs/14`：A1/A2 + 禁四类）+ **四属性校验** + **yaml 指纹门禁**（`packs/heat_kefu`）
- **序列命中**（T33：多分句整段拆句铸入 + 逐字覆盖，真链路实弹 T34 字节级同参照）
- 槽位模板 ✅ / 多语速档 ✅
- **双工四参数被消费**（T19）+ **双工质量指标**（T20：打断准确率/假阳性/轮转延迟，固定种子可复现）
- **TTL / 失效**（T18，**自 M3 前移**——过期资产拒播，`allow_fallback` 也不放行）
- **真链路命中**（T30 钩子 + T34 序列档实弹：`ttsEngine=wav`、与参照 sha256 逐字节相同）
- **热路径索引化**（T27：10k 条下 0.5–0.8 µs，为"量大以后"铺路）
- **开源**：Apache-2.0，发布仓 `github.com/starmcainiao/vox-type`，CI 三档矩阵全绿

### M3 · 回流与盲测（**部分完成**）
- **回流闭环** ✅ **已交付（T23）**：`tools/feedback_mining/`（留痕+事件流 → 候选 → 形式条款 + 待人工归类 → 对接 `docs/14` 申报）
- **盲测自然度报告**（需真人听测 → 与 `golden` 验收集 B 同批）
- **端到端对照臂** ✅ **已交付（T24）**：`labs/e2e-vs-cascade/`——组合版命中 0.1ms/逐字 1.0 vs 未命中 4256ms/逐字 0.0；云端 omni 端到端首字节 717ms、音频字节 8 种取值（不保证逐字）

## 三、修订依据（可追溯）

| 结论 | 出处 |
|---|---|
| 语义命中证伪（0.00% / 11.8%） | `docs/11 §11.9`、`labs/corpus-harvest/README.md §十`（对账表，命令可重跑） |
| 预铸适用边界 = 流程决定话轮（23%） | `docs/11 §11.9`、`docs/16 §四` |
| 双工交付（T19/T20） | `docs/tasks/00-索引.md` 第二十一批 |
| TTL 交付（T18） | 同上 |
| 序列命中 + 真链路实弹（T33/T34） | `docs/tasks/00-索引.md` 第二十批、`labs/kefu-seq-e2e/` |
| 索引化（T27） | `labs/pack-index-bench/` |
| 未完成项对照 | `docs/13-未完成清单.md §六`（对外交付缺口） |

## 四、对外一句话

> **M0/M1 完成；M2 已交付**——但以「**可审核的预铸资产层 + 双工行为控制器**」替代原计划的「语义命中」
> （后者经 2433 条真实语料实测证伪，本项目不做近似匹配）；
> **M3 三项已交 2 项**：回流闭环 ✅（T23）、端到端对照臂 ✅（T24）；
> **仅剩盲测自然度**——素材与打分表已就绪（`labs/naturalness-ab/`），等真人听测填表出报告，
> 状态见 `docs/13 §六#4`。
