# 18 · 开源就绪评估（2026-09-19，本轮清理后）

> **结论一句话：已开源（2026-09-21，第十八批）**——`github.com/starmcainiao/vox-type`（Apache-2.0，
> 干净单提交快照，CI 三档矩阵）。**本文件是当时（2026-09-19）的评估记录**；此后第二十/二十一批又交付了
> 冻结区 4 卡（T27/T19/T18/T20，见 `00-索引`），条件 2 的测试条数已刷新（见下表）。差距清单见 §三。

---

## 一、开源纪律逐条核对（对照总纲开源纪律）

| # | 条件 | 状态 | 证据 |
|---|---|---|---|
| 1 | 真实使用过 | ✅ | kefu 真链路集成（T11/T30）+ 实弹 4 轮命中（第十五批） |
| 2 | 机制稳定 | ✅ | 十一测试根 **1,547 条全绿、0 失败**（2026-09-23；packs 的真包断言在 CI 已加构建步）——T39 清掉了 T18 遗留的 4 条 TTL 用例（夹具吃墙钟 + `executor._diagnose` 判据源不一致，`docs/13 §五#22`）后**本仓首次全绿**；端到端闭环 + 真链路命中 + **序列档真链路实弹**（T34）+ 本地克隆音色重铸（T35） |
| 3 | 敏感数据清理 | ✅（本轮完成；**2026-09-23 T36 复核复扫**） | 六类扫描清零：密钥/token（0）、隐私词（0）、**绝对路径**（外部卷挂载前缀、启动卷 tmp 前缀、家目录前缀——一律 0）、用户名（0）、非回环 IP（仅测试 fixture 10.0.0.5）、`.env`（不存在）。**裁定（09-23 复核）：这些前缀的「字面写法」本身也不写进仓**——写下来等于让扫描永远非零，且每次都要重新裁定「那是路径还是检查项的名字」 |
| 4 | 无第三方源码混入 | ✅ | 仓内无 reference/ 目录；零第三方依赖（纯标准库） |
| 5 | License | ✅ | Apache-2.0（LICENSE 在根）；fin-cs 源语料 MIT/Apache 兼容（话术为改写非复制）；语料台账即 attribution（corpus.lock.json + README 归属节） |

## 二、本轮清理动作（三个提交）

- `4272b51`（前批）：审计补账 + adapters §⑨ 登记；
- `585f998`：**代码三处硬编码本机路径改 env 优先**（`KEFU_AGENT_ROOT` / `CORPUS_ROOT` /
  `KEFU_HEAT_YAML`，labs 语料根统一 env+expanduser，1.8% 数字回归 PASS）；**00-索引 商汤配置段脱敏**；
  隐私词/体系名中性化；55 个 md + 15 个 labs/tools 文件绝对路径中性化；**README Quick Start +
  第三方语料归属声明**；kefu_client 校验顺序修正（脚本先于解释器，开源机器上不再误报）；
- `758250a`：终清——外部卷绝对路径、商汤端点、体系名、隐私词全部出仓
  （`git grep` 终验：六类关键词——用户名 / 本机绝对路径 / 内部端点 / 隐私词 / 体系名——命中文件数全部归零；本节措辞已同步脱敏，不回写关键词原文。）

## 三、还差的（按是否阻塞 push 分级）

### 阻塞级（push 前必须拍板）

| # | 事项 | 说明 | 推荐 |
|---|---|---|---|
| 1 | **git 历史包袱** | 当前树干净，但 90+ 提交的**历史**里仍有敏感内容（商汤端点调试记录 3 个提交、大量本机绝对路径、体系名）。推 GitHub = 连历史公开 | **squash 单提交发布**：以当前树为初始提交推到新公开仓，完整历史留在本地私有仓（零重写成本，最干净）。备选：git filter-repo 重写（保留粒度但 hash 全变、工序重） |
| 2 | **push 本身** | 开源红线：必须用户拍板 + 建远端 | 等用户指令（仓名/GitHub 账号/公开时机） |

### 非阻塞（开源后可迭代）

| # | 事项 | 说明 |
|---|---|---|
| 3 | 中文注释与文档 | 全仓中文注释——受众取舍。保留（技术内容自解释），后续可加英文 README 摘要 |
| 4 | CI | GitHub Actions 跑十一根（脚本即 `for d in …; do unittest discover`，开源后半小时接上） |
| 5 | 内部治理项 | cli 层无独立 AGENTS.md 的口径拍板；docs/12 旧世代收缩；00-索引 批次归档——均登记在 docs/13/结构审计，不阻塞 |
| 6 | labs 的labs 性脚本 | labs/ 按纪律是实验区（无测试根），开源无碍；#12 的对账表已提供数字复现入口 |

## 四、对外叙事配套（已就绪）

- `docs/16`（痛点验证）、`docs/17`（业界坐标与改造预测）、`docs/14`（准入判据）、
  `docs/15`（brain 按 key 说话方案）——四篇构成完整对外叙事链，均已含来源；
- 讲法稿在内部仓求职面试包（含数字备查口径），不随本仓公开。
