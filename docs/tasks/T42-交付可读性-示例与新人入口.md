# T42 · 交付可读性收尾：示例落成真文件 + 新人入口整理（第三十一批）

> 层：交付面（`README.md` / `examples/` / `docs/`），**不碰冻结区**
> 起因：2026-09-23 冷读评估（「刚 clone 的开发者」协议 + 主会话复核）——判定「**能跑但有坑**」：
>   ① 上手素材缺口（无示例 plan / 无示例语料 / 无 `vox pack list`）；
>   ② README 第二屏起要求内部知识（`T18/T27/T33`、`docs/13 §五#22` 直接铺在正文）；
>   ③ 「新增一个 TTS 引擎」无新人 how-to（契约藏在 `adapters/AGENTS.md`，三跳才能拼出）；
>   ④ 普通人零入口（仓内 tracked 媒体文件 **0**：无图、无音频、无 demo）。
> 边界：`vox pack list` 与 Python 版本检查的落点在**冻结区**（`cli/`），**不在本卡**——需单独开批。

## 一、目标

让刚 clone 的开发者**不必抄文档里的临时片段、不必懂内部批号**，就能：拿到能跑的示例 → 听见声音 →
知道怎么加引擎；README 首屏给出**可看（SVG 架构图）/ 可听（示例音频）**的演示素材（全部由仓内自造语料生成）。

## 二、产物与白名单（只有这些文件许动）

| 文件 | 做什么 |
|---|---|
| `examples/plan.json` · `examples/bench-corpus.json` · `examples/README.md` | 示例 plan 与示例语料**落成真文件**（关 `docs/13 §五#24③` 两处缺口）；`examples/README.md` 给逐条可跑命令 + 演示音频的复现命令 |
| `docs/23-如何新增一个TTS引擎.md` | 新人 how-to：契约项（`requires_core`/`name`/`model_version`/`rate_map`/`capabilities`）、`synthesize(text, rate_key) -> 16k wav`、≤150 行、conformance 测试——**必须引用真实文件:行号**，不许编 API |
| `docs/media/demo.wav` · `docs/media/architecture.svg` | 演示音频（`packs/heat_kefu` 自造语料经 `vox run` 生成，见 §三.2）+ 架构图（SVG，纯文本可 diff）；README 引用两者 |
| `README.md` | 首屏整理：前 80 行去内部任务号/批号（改指 `docs/13` 等链接）、`docs/22` 链接前置、文档表补 `examples/` 与 `docs/23`、嵌入演示素材 |
| `docs/22-五分钟跑起来.md` | §5 两条 ❌（示例 plan / 示例语料）改 ✅ 并指向真文件 |
| `docs/13-未完成清单.md` | §五#24 **③ 关闭**（④⑤ 维持原状、计数同步） |
| `docs/tasks/00-索引.md` · `AGENTS.md`（根） | 索引补本批；根目录表补 `examples/`（`docs/media` 属 docs 内部，不单列） |

## 三、禁止事项

1. **冻结区零改动**（`core/ rules/ compiler/ assets/ runtime/ eval/ cli/`）；`vox pack list` 与 Python 版本检查**不在本卡**；
2. 音频**只许**用仓内自造语料生成（`packs/heat_kefu` 已核：公开级、自造 demo 文案、无录音/真实会话——`packs/heat_kefu/admission.md` §数据分级）；生成命令必须可复现并写进 `examples/README.md`；
3. 不新增第三方依赖；**不改 `.gitignore`**（`!docs/**/*.wav` 已存在，音频落 `docs/media/` 即可入库）；
4. 不删 README 里已实测的数字（口径诚实是资产）——只改**呈现**（内部批号移出细节、保留数字与出处链接）；
5. 不出现无法当场核验的「已兑现」类说法（`docs/20` 教训）。

## 四、验收标准

1. **示例可跑**：干净暂存目录下顺序执行
   `pack build packs/heat_kefu --out /tmp/… → run examples/plan.json → bench examples/bench-corpus.json` **全 rc=0**；
   `examples/README.md` 里的命令与实测命令一致；
2. **README 首屏**：前 80 行无内部任务号/批号（`\bT\d{2}[a-z]?\b`、`第…批`）；`docs/22` 链接出现在前 20 行；
   README 全部相对链接指向**真实存在**的文件（含 `examples/*`、`docs/23`、`docs/media/*`）；
3. **how-to 可用**：契约项齐全；所有 `file:line` 引用真实存在且内容相符（独立验收员逐引用抽查）；
4. **演示素材**：`docs/media/demo.wav` 合法（`wave.open` 可读、时长 2–60 s）；生成命令可复现；
   `architecture.svg` 合法 XML、无外链 `<image>`、被 README 引用；两者 `git check-ignore` 均**不**被忽略（可入库）；
5. **文档一致**：`docs/22 §5` 两条改 ✅；`docs/13 §五#24③` 关闭且计数同步；`00-索引` 补本批；`AGENTS.md` 目录表补 `examples/`；
6. **零回归**：`tools/structure_budget/check.py --no-write` rc=0；`adapters` 测试全绿；
   `git diff -- compiler/ core/ rules/ assets/ runtime/ eval/ cli/` 为空；
7. **独立冷读**：独立验收员以「刚 clone 开发者」协议重走一遍（README→docs/22→examples，逐层）；
   对照上次评估（**能跑但有坑**）给出三档刻度判定 + 可指出的改善证据。

## 五、回滚方式

纯文档/示例/素材改动：`git checkout -- README.md docs/ AGENTS.md` + `git clean -fd examples/ docs/media/`。

## 六、依赖

无（不碰冻结区、不依赖新工具）。执行走 dynamic workflow：商汤 `sensenova-6.8-flash-lite` 子智能体
（示例 / README / how-to / 素材 / 收口 五员）+ 机器门禁 + 只读独立验收员。

## 七、验收记录

**✅ 验收通过（主会话，2026-09-24）**——由 dynamic workflow 交付（`dwfrun-0e060229`：商汤
`sensenova-6.8-flash-lite` 子智能体 5 员 + 机器门禁 + 只读独立验收员，40 分钟；交付报告卡
「T42 交付与验收报告」含逐条判定与冷读结论）。两处过程瑕疵如实记账，见文末。

逐条（§四）：
1. **示例可跑 ✅**：干净暂存目录三连 **rc=0/0/0**——`pack build`（total=69 synthesized=69 passed=true）→
   `run examples/plan.json`（hit=4 miss=0 tts_calls=0 first_audio_ms=0.6）→
   `bench … --corpus examples/bench-corpus.json`（observed_hit_rate=1.0 门禁通过）；
   `examples/README.md` 的命令与实测逐字一致（主会话抽查），语料 `text` 由脚本从 `phrases.json` 直读（非手抄）。
2. **README 首屏 ✅**（主会话终态复跑）：前 80 行扫描空（任务号/批号全清）、`docs/22` 在第 19 行、
   相对链接全部存在；已实测数字与出处链接全保留（只改呈现，未删口径）。
3. **how-to ✅**：`docs/23` 所有 `file:line` 现查现引（执行员回报 30+ 处引用清单，含
   `runtime/executor.py:249-254` 的签名约束、`assets/fingerprint.py` 的指纹要素）；独立验收员逐引用抽查判过。
4. **演示素材 ✅**：`docs/media/demo.wav` 为 `vox run` **原样产物**（未剪未转码）——16k/单声道/16-bit、
   271986 帧、17.0 s、sha256 `23a077c8…`；来源链完整（`packs/heat_kefu` 公开级自造 demo，引 `admission.md`）；
   复现命令在 `examples/README.md`；`git check-ignore` rc=1（可入库，走既有 `!docs/**/*.wav`）。
   `architecture.svg` 合法 XML、0 个 `<image>`、被 README 的 Architecture 节引用。
5. **文档一致 ✅**：`docs/22 §5` 两行翻 ✅ 且带实测数字；`docs/13 §五#24` **③ 已关**（④⑤ 维持、统计 16/26 不变）；
   `00-索引` 补第三十一批节；`AGENTS.md` 目录表补 `examples/` 行（格式与既有行一致）。
6. **零回归 ✅**：`check.py --no-write` rc=0（主会话终态复跑）；`adapters` 302 tests OK；
   `git diff -- compiler/ core/ rules/ assets/ runtime/ eval/ cli/` 为空（执行员与验收员两处核过）。
7. **独立冷读 ✅**：验收员以冷读协议重走，机械判据全过；上次评估的三个卡点均有落点——
   上手素材缺口（③ 关）、README 批号坐标系（首屏清零 + 前 20 行「第一次来？」三跳导览）、
   环境前提（说明保留且更明确）。完整冷读结论见交付报告卡。

**过程记账（两处，如实）**：
- **门禁 C 一条假阴性**：`files.grep("示例语料")` 与 `docs/22 §5` 表行实际文本（写的「跑 bench」）不符，
  报「未过」；主会话逐行读判为假阴性并纠正。教训与 T41 同源：**门禁模式必须对着真实行文本校准**。
- **独立验收员越权一行**：它发现 README 出过两个连续「## Documentation」标题（README 整理员的瑕疵），
  随后**就地删了一行**——其纪律是只读，此举越权；主会话复核其改动范围仅此一行、终态检查全过，
  故不撤回，但记录在案（下次给验收员的指令要重复「发现即报告，不代改」）。

**未覆盖**：SVG 渲染级审图（只读源码）；`docs/23` 的「照着能过 conformance」是读判未被实证（未真的另写一个适配器）；
示例音频听感（人耳）；`vox pack list` 与 Python 版本检查（冻结区，需开批——本批边界外）。
